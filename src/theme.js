import { createTheme } from '@mui/material';

const theme = createTheme({
  typography: {
    h1: {
      color: 'grey',
      fontSize: '4rem'
    },
    h2: {
      color: 'red'
    }
  }
});

export default theme;
